.env
.env.*
!.env.example
node_modules/
reports/
.trends/
*.log

# Packed tarballs
*.tgz

# Editor-local
.vscode/

# Working documents: partner field-test write-ups, the roadmap they were triaged into, and
# the review backlog. They quote partner systems and internal review threads, so they stay
# local — the public record of what shipped and why is CHANGELOG.md.
docs/
.trust-credentials.env

# Registry credentials
.npmrc
