# TRUST — secrets live here only. Config files store env var NAMES, never values.
# Copy to .env and fill in. .env is gitignored.

# ── API / GraphQL identities (two users for isolation testing) ──
AUTH_TOKEN_A=
AUTH_TOKEN_B=

# ── Storage identities ──
STORAGE_TOKEN_A=
STORAGE_TOKEN_B=

# ── AI agent runtime identities ──
AGENT_TOKEN_A=
AGENT_TOKEN_B=
